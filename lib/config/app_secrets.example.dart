/// Copy this file to `app_secrets.dart` and fill in your own values.
///
/// IMPORTANT: `app_secrets.dart` is gitignored and must NOT be committed.
class AppSecrets {
  static const supabaseUrl = 'YOUR_SUPABASE_URL';
  static const supabaseAnonKey = 'YOUR_SUPABASE_ANON_KEY';
}

